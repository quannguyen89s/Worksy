/**
 * Base URL backend — thống nhất với @/config/api (EXPO_PUBLIC_API_URL, IP từ Expo).
 * Giữ tên API_BASE để import cũ (jobApi, authService) vẫn hoạt động.
 */
export { API_BASE_URL as API_BASE } from '@/config/api';
